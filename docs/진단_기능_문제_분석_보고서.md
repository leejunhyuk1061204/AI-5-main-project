# 진단 기능 문제 분석 보고서 (Part 1)

## 문제 발생 경위

**커밋**: `62f2943`  
**작성자**: hyun (강현님)  
**날짜**: 2026-02-02 16:10:35  
**커밋 메시지**: "로그아웃 시 store 초기화"

## 변경된 파일 목록

### 백엔드
- `backend/src/main/java/kr/co/himedia/repository/DiagResultRepository.java`
- `backend/src/main/java/kr/co/himedia/service/AiDiagnosisService.java` ⚠️

### 프론트엔드
- `frontend/diagnosis/AiDiagChat.tsx`
- `frontend/diagnosis/EngineSoundDiag.tsx`
- `frontend/diagnosis/VisualDiagnosis.tsx`
- `frontend/diagnosis/DiagMain.tsx` ⚠️
- `frontend/diagnosis/ObdDiagLoading.tsx` ⚠️ (완전 재작성)
- `frontend/filming/Filming.tsx`
- `frontend/store/useAiDiagnosisStore.ts`
- `frontend/store/useAlertStore.ts`
- `frontend/store/useUserStore.ts`
- `frontend/store/useVehicleStore.ts`

## 핵심 문제

### 1. AiDiagnosisService.java (406번째 줄)
```java
// 이전 (작동하던 버전 - a3349e3)
Map<String, Object> finalResponse = aiClient
    .callComprehensiveDiagnosis(objectMapper.convertValue(aiRequest, Map.class));

// 현재 (더미 버전 - 62f2943)
// 3. 최종 통합 진단 요청 (Phase 1: Mock Response)
log.info("[Initial] Mocking comprehensive diagnosis response for testing.");
Map<String, Object> finalResponse = new HashMap<>();
finalResponse.put("response_mode", "REPORT");
finalResponse.put("summary", "차량의 상태를 분석한 결과, 엔진 오일 압력 센서...");
// ... 하드코딩된 더미 데이터
```

### 2. DiagMain.tsx (72번째 줄~)
```typescript
// 이전 (작동하던 버전)
if (pendingAction === 'OBD') {
    await startDiagnosis(vehicle.vehicleId);  // ← 실제 백엔드 API 호출
}

// 현재 (더미 버전)
if (pendingAction === 'OBD') {
    navigation.navigate('ObdDiagLoading', { vehicleId: vehicle.vehicleId }); // ← 단순 화면 이동
    // await startDiagnosis(vehicle.vehicleId); // 주석 처리됨!
}
```

### 3. ObdDiagLoading.tsx
- **완전히 재작성됨** (실제 데이터 폴링 로직 삭제)
- 정적 UI만 남음 (진행률 67% 고정, "DTC 고장 코드 스캔 중..." 텍스트만 표시)

---

## AI 서버 연동 구조 확인

### test_router.py
- **경로 1**: `/connect/predict/comprehensive` - Mock 응답 (사용자 테스트용)
- **경로 2**: `/test/predict/yolo` - 실제 YOLO 추론
- **경로 3**: `/test/predict/audio` - 실제 AST 추론

### 현재 아키텍처
- **visual (사진 진단)**: 실제 AI 연동 필요
- **sound (소리 진단)**: 실제 AI 연동 필요
- **데이터 진단 (OBD)**: test_router의 `/connect/predict/comprehensive` 사용

## 복원 계획

### 복원 대상 파일
- `backend/src/main/java/kr/co/himedia/service/AiDiagnosisService.java`
- `frontend/diagnosis/DiagMain.tsx`
- `frontend/diagnosis/ObdDiagLoading.tsx`

### 복원 방법
```bash
# 커밋 a3349e3 (수동 자동 진단 통합) 버전으로 복원
git checkout a3349e3 -- backend/src/main/java/kr/co/himedia/service/AiDiagnosisService.java
git checkout a3349e3 -- frontend/diagnosis/DiagMain.tsx
git checkout a3349e3 -- frontend/diagnosis/ObdDiagLoading.tsx
```

## 사용자 확인 필요 사항
1. **test_router 연동 범위**: 데이터 진단만 `/connect/predict/comprehensive` 사용하는 것이 맞는지?
2. **UI 개선 유지**: `EngineSoundDiag.tsx`, `VisualDiagnosis.tsx`의 UI 개선은 유지하는 것이 맞는지?
3. **복원 후 테스트**: 복원 후 실제로 진단 기능이 정상 작동하는지 검증 필요
