# --- 1. Web Server (FastAPI 필수) ---
fastapi>=0.100.0
uvicorn[standard]>=0.20.0
python-multipart    # 파일 업로드(이미지/오디오) 처리에 필수!
pydantic>=2.0.0

# --- 2. AWS 연동 (S3 쓰려면 필수) ---
boto3               # 파이썬으로 AWS S3에 접속하는 표준 도구

# --- 3. 데이터 처리 (공통) ---
numpy
pandas

# --- 4. 시각 (YOLOv8) ---
ultralytics         # YOLOv8 공식 라이브러리
# [중요] 서버에서는 화면이 없으므로 opencv-python 대신 이걸 써야 에러 안 남!
opencv-python-headless 

# --- 5. 청각 (Audio 전처리) ---
librosa             # 오디오 분석 표준 라이브러리
soundfile           # 오디오 파일 읽기/쓰기

# --- 6. 정형/시계열 (XGBoost, LSTM 등) ---
scikit-learn        # 데이터 전처리(StandardScaler 등)에 필요
xgboost             # (나중에 쓸 수 있으니 미리 넣어둠) 