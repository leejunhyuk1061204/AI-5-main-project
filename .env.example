# Database Configuration
DB_USER=...
DB_PASSWORD=...
DB_NAME=car_sentry
DB_PORT=5432

# RabbitMQ Configuration
MQ_USER=...
MQ_PASSWORD=...
MQ_PORT=5672
MQ_MANAGEMENT_PORT=15672

# AI Server 설정 (FastAPI)
# APP_ENV: local (테스트 모드), production (운영 모드)
APP_ENV=local

# Storage 설정 (Backend)
STORAGE_TYPE: local (로컬 디렉토리), s3 (AWS S3)
STORAGE_TYPE=local

# AI Server URL (Backend -> AI)
AI_SERVER_URL_VISUAL=http://localhost:8000/api/v1/predict/vision
AI_SERVER_URL_AUDIO=http://localhost:8000/api/v1/predict/audio

# 1. 실행 중인 컨테이너 중지 및 삭제
# docker stop car-sentry-redis
# docker rm car-sentry-redis
# 2. 저장된 데이터(볼륨) 삭제
# docker volume rm ai-5-main-project_redis_data
# 3. (선택사항) redis 이미지 삭제
# docker rmi redis:7.2-alpine
